// Nombre de messages TchopAI débloqués par pub récompensée visionnée.
// Utilisé pour le décrément du quota ET l'affichage (placeholder {count} des traductions).
export const REWARDED_MESSAGES_PER_AD = 1;

// Nombre maximum de pubs récompensées par jour (borne les coûts Claude).
export const REWARDED_DAILY_LIMIT = 3;
